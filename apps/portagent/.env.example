# Portagent Configuration
# Copy this file to .env and fill in the values

# Required: LLM Provider API Key
LLM_PROVIDER_KEY=sk-ant-xxxxx

# Optional: LLM Provider base URL
# LLM_PROVIDER_URL=https://api.anthropic.com

# Optional: LLM Model name (default: claude-sonnet-4-20250514)
# LLM_PROVIDER_MODEL=claude-sonnet-4-20250514

# Optional: Server port (default: 5200)
# PORT=5200

# Optional: Login password (auto-generated if not set)
# PORTAGENT_PASSWORD=your-secure-password

# Optional: JWT secret for token signing (auto-generated if not set)
# JWT_SECRET=your-jwt-secret

# Optional: Log level (debug, info, warn, error)
# LOG_LEVEL=info
